#import rtconfig
#Import('RTT_ROOT')
from building import *

# get current directory
cwd = GetCurrentDir()

# The set of source files associated with this SConscript file.
src = Split("""
sort/flush_left/flush_left.c
sort/char_swap/char_swap.c
""")

path = []

#path = [cwd + '/STM32L1xx_StdPeriph_Driver/inc', 
    #cwd + '/CMSIS/Device/ST/STM32L1xx/Include']

path += [cwd + '/../include']
path += [cwd + '/../../../include']

CPPDEFINES = []
group = DefineGroup('bsp_library', src, depend = [''], CPPPATH = path, CPPDEFINES = CPPDEFINES)

Return('group')
