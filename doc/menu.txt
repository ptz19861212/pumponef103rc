进行设置的时候，当输入数值的时候，输入数值结束的时候数据应该放入临时数据，
因为下面还有YES和NO的选择，如果按下YES才能把临时数据放入SAVE数据，按NO的话
就要用SAVE数据重写临时数据。最后等菜单退出了以后才能把所有的SAVE数据一次
写入FLASH。
每次进入菜单模式的时候把FLASH里面的数据读入SAVE数据，然后进每个具体菜单的
时候都使用SAVE数据
在输入模式读取的应该是临时的数据，不然的话，每次YES和NO的时候，数据又变回
原来的数据了，这样的话，就同时要求在进入具体菜单的时候，需要把SAVE数据先
写入一次临时数据
